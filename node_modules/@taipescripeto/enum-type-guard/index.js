"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Enum Type Guard
 */
function enumTypeGuard(value, enumeration) {
    var enumAsObject = Object(enumeration);
    //  includes is not compatible with some browsers
    var indexNotFound = -1;
    return Object
        .keys(enumeration)
        .map(function (key) { return enumAsObject[key]; })
        .indexOf(value) !== indexNotFound;
}
exports.enumTypeGuard = enumTypeGuard;
